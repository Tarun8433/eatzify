name: health_pro
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # required by CLAUDE.md rules 4 & 5 (no raw enums, no hardcoded strings) — doc 20 §7 lists
  # `intl` but l10n cannot work without this SDK package
  flutter_localizations:
    sdk: flutter

  # State, routing, DI — your existing stack
  get: ^4.6.6
  dartz: ^0.10.1

  # Network & storage
  dio: ^5.7.0
  hive_ce: ^2.0.0              # hive is unmaintained; hive_ce is the community fork
  flutter_secure_storage: ^9.2.2

  # Health — HealthKit (iOS) + Health Connect (Android) behind one API.
  # Dropped Google Fit at v11.0.0 because Google closed signups (confirms doc 00 §4).
  health: ^13.3.1

  # Billing — see doc 20 §9 for the entitlement-ownership caveat
  purchases_flutter: ^8.0.0    # RevenueCat
  razorpay_flutter: ^1.4.0     # web/UPI checkout

  # UI
  flutter_screenutil: ^5.9.3
  fl_chart: ^0.69.0            # weight trend, macro rings
  lottie: ^3.1.3               # the "preparing your plan" animation after onboarding
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0              # skeleton loaders (NFR-8)
  cupertino_icons: ^1.0.8

  # Logging & scanning
  # mobile_scanner: ^6.0.0     # barcode → Open Food Facts. REMOVED, not abandoned: its GoogleMLKit
  #   pods ship no arm64 simulator slice, so with it in the Podfile Xcode offers no simulator
  #   destination at all and `flutter run` cannot launch on any Apple Silicon simulator. Nothing in
  #   lib/ imports it yet. Add it back with the barcode screen — by then either MLKit ships arm64
  #   simulator support, or the screen is developed on a physical device.
  image_picker: ^1.1.2

  # Notifications
  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.10.0            # required for correct IST reminder scheduling

  # Platform
  intl: ^0.20.2                # doc 20 §7 says ^0.19.0; Flutter SDK flutter_localizations pins 0.20.2
  package_info_plus: ^8.1.0    # fixes the hardcoded "Version 1.0.0" (doc 15 D-07)
  google_sign_in: ^7.2.0
  intl_phone_field: ^3.2.0
  google_fonts: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  freezed: ^2.5.7              # immutable entities + copyWith, per doc 14 §3
  mocktail: ^1.0.4
  golden_toolkit: ^0.15.0
  very_good_analysis: ^6.0.0   # stricter lints than flutter_lints (doc 20 §7)

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  assets:
    # D-55: the Home walker. 38 palette PNGs, 492 KB total.
    - assets/walking/
    - assets/activity/
    # D-75: played while the first plan is generated. Quoted for the space in the filename.
    - "assets/Preparing Food.json"
    # D-102: the welcome carousel's full-bleed art, and the meal-slot headers. Supplied as ~30 MB
    # of PNG and re-encoded to 1.7 MB of JPG at display resolution — a photo stored as PNG is
    # lossless data nobody can see, and in this app's market bundle size is install conversion.
    - assets/onboarding/
    - assets/welcome/
    - assets/meal_slots/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
