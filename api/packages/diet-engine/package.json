{
  "name": "@eatzify/diet-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Pure, deterministic diet planning engine. No I/O, no clock, no random, no DB, no HTTP.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "test:cov": "jest --coverage",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/js-yaml": "^4",
    "@types/node": "^22",
    "jest": "^29",
    "js-yaml": "^4",
    "ts-jest": "^29",
    "typescript": "^5"
  },
  "files": [
    "dist"
  ]
}
