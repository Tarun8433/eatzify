ADMIN_API_URL=http://localhost:3001/api/v1
# The dashboard signs in as the person using it (D-234). There is deliberately no
# service account here any more: one set of credentials in a file was the same as no login.
